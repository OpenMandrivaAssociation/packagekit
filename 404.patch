diff -up PackageKit-1.3.3/policy/org.freedesktop.packagekit.rules.2~ PackageKit-1.3.3/policy/org.freedesktop.packagekit.rules
--- PackageKit-1.3.3/policy/org.freedesktop.packagekit.rules.2~	2025-11-24 20:20:39.000000000 +0100
+++ PackageKit-1.3.3/policy/org.freedesktop.packagekit.rules	2025-12-14 00:27:16.795739812 +0100
@@ -2,7 +2,9 @@
 // without a password prompt.
 
 polkit.addRule(function(action, subject) {
-    if ((action.id == "org.freedesktop.packagekit.upgrade-system" ||
+    if ((action.id == "org.freedesktop.packagekit.package-install" ||
+         action.id == "org.freedesktop.packagekit.package-remove" ||
+         action.id == "org.freedesktop.packagekit.upgrade-system" ||
          action.id == "org.freedesktop.packagekit.trigger-offline-update" ||
          action.id == "org.freedesktop.packagekit.trigger-offline-upgrade") &&
          subject.active == true && subject.local == true &&
